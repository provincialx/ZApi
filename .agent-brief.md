# FreeQwenApi Proxy — Agent Brief (Session 2026-06-07)

## Архитектура
FreeQwenApi = браузерный прокси ForgetMeAI. Node.js + Puppeteer.
*   Заменяет веб-аккаунт Qwen Chat на локальный OpenAI-compatible API (`localhost:3264`)
*   Эмулирует вызовы Qwen через JS `fetch` внутри puppeteer-страницы
*   Zed Agent (Browser) → FreeQwenApi → Qwen Chat (через браузер) → ответ обратно
*   Цель: Полноценный агент с tool_calls через интерфейс Zed. Терминальные обёртки исключены.

## Zed.dev + chat.qwen.ai (что узнали)
### Zed
- Rust-based редактор, GPU-accelerated GPUI
- Agentic Editing: встроенные AI-агенты с параллельным выполнением, MCP-серверы
- Edit Prediction через Zeta2 модель. Vim-friendly modal editing
- Any Agent, Any Tool — поддерживает ACP (Agent Communication Protocol), Claude/Codex/OpenCode agents
- Подключается к OpenAI-compatible API: `base_url`, `api_key` в settings

### Qwen Studio (chat.qwen.ai)
- Веб-чат Qwen. Mobile-first, требует скачивания App на десктопе ("Current System does not Support")
- Для прокси не важно — мы стучимся к `/api/v2/chat/completions` через Puppeteer, а не к фронтенду

## Найденные проблемы
### Сессия 1 (решены)
1. **`resolveQwenChatId` без return** → функция возвращала `undefined`, маппинг чатов не сохранялся.
2. **Орфан-импорт посреди файла** (`import { testToken }`) → нарушало структуру JS модуля.
3. **«Потрошное месиво» в логах** (273 сообщения) → бесконечная строка ролей забивала консоль.
4. **Смерть сессий (`chat_not_exist`)** → Qwen удалял старые чаты, прокси использовал кэшированный ID.
5. **Неряшливый лог ошибок** (`undefined`) → `handleApiError` возвращал falsy `{ error: undefined }`.

### Сессия 2 (решены сегодня)
6. **Отсутствие `protocolTimeout` в puppeteer.launch()** → дефолт 180с убивает большие задачи.
7. **Бесконечный hang health-check пула страниц** → `page.evaluate()` без таймаута.
8. **Нет retry при сетевых ошибках goto** → мгновенный фейл на `ERR_CONNECTION_RESET`.
9. **Sliding window рвёт tool_call пары** → `.slice(-40)` мог оставить orphan tool без assistant context.

### Сессия 3 (решены сегодня)
10. **SyntaxError L1287 — незакрытый object literal** → Node ронялся при старте.
11. **applyToolPrompt не принимал `inAgentLoop`** → agent-loop с tool results получал агрессивный prompt вместо light mode. Фикс: `toolsToLightPrompt()` + 3-й параметр.

### Сессия 4 (решены сегодня)
12. **Warn "модель игнорирует инструменты" не работал** → check стоял в chat.js по параметру `tools` который всегда `null`.

### Сессия 5 (решено сегодня)
13. **Портянка километровая в логах** → `logRaw()` дампил полные JSON-тела, debug-trace middleware спамил на каждый запрос к `/chat*`, LOG_LEVEL дефолт = `'debug'`. Всё убрано.

### Сессия 6 (текущая — 2026-06-07 13:xx)
14. **`logWarn is not defined` в routes.js L1498** → ReferenceError crash на streaming пути. Фикс: добавить `logWarn` в import L25.
15. **Qwen возвращает ОБЫЧНЫЙ ТЕКСТ вместо JSON tool_calls** → при `captureToolCalls=true` (stream + tools present), модель пишет "Файл создан..." без `{"tool_calls":[...]}`. parseToolCallJson получает текст, не парсит JSON, returns null.
16. **Double-injection tool prompt в buildPayloadV2** → system_message уже содержал prompt от routes.js (applyToolPrompt вызван ДО sendMessage). buildPayloadV2 вызывал applyToolPrompt ВТОРОЙ раз → дублирование инструкций, возможная путаница модели. Фикс: убран вызов, оставлен debug log.

### Сессия 7 (текущая — 2026-06-07 13:xx)
17. **CAPTURE MODE debug подтвердил: контент = TEXT без JSON** → добавил лог в chat.js L1263 при captureToolCalls. `content type=string, len=275-303, preview="Файл ...создан."`. Никакого `{` в ответе Qwen. Модель полностью игнорирует prompt "respond ONLY with minified JSON".
18. **Tool prompt experiment: reasoning-first FAIL** → попробовал изменить prompt на "напиши рассуждение СНАЧАЛА, затем JSON внизу". Result: модель пишет ТОЛЬКО текст, JSON не генерирует вообще. Вернул агрессивный "respond ONLY with minified JSON" — но даже он НЕ работает при tool history в контексте.
19. **buildStatelessTranscript отправлял сырой JSON-блоб Qwen** → `JSON.stringify(msg.tool_calls)` давал 120+ символов необработанных данных, которые Qwen не парсит и воспринимает как шум. Фикс: заменил на `"Assistant used tools: write_file"` (натуральный язык). Аналогично tool results → `"Tool result (write_file): ..."`.

## Применённые патчи
| # | Фикс | Файл | Суть |
|---|------|------|------|
| 1–4 | resolveQwenChatId + handleApiError + auto-retry chat_not_exist + sliding window | routes.js, chat.js | См. проблемы 1-5,9 выше |
| **5** | protocolTimeout 10min + env override | `browser.js` | `protocolTimeout: Number(process.env.PROTOCOL_TIMEOUT) || 600_000` |
| **6** | evaluate timeout 5s (Promise.race) | `chat.js` | `evaluateWithTimeout()` — CDP-зависание → страница помечается мертвою за ≤5с |
| **7** | Retry goto с exponential backoff | `chat.js` | До 3 попыток с `RETRY_DELAY * attempt`. Сеть нестабильна → больше не фейлит мгновенно. |
| **8** | Sliding window: сохранять tool_call пары | `routes.js` | Если после `.slice(-40)` первая message — `role: "tool"`, добавляется assistant с `tool_calls`. Qwen видит полный контекст вызова. |
| **9** | Fix tool_calls (Prompt Injection + Post-parse) | `chat.js, routes.js (+toolUtils.js)` | Qwen Chat API не принимает OpenAI `tools[]`. Инжектируем в System Message через prompt (`applyToolPrompt`). Ответ парсится обратно в OpenAI `message.tool_calls` через `parseToolCallJson`. Централизованно в `src/api/toolUtils.js`. Smoke test пройден. |
| **10** | Agent-loop: light tool prompt | `toolUtils.js, routes.js` | `applyToolPrompt()` принимает 3-й аргумент `inAgentLoop`. При наличии tool results → лёгкий prompt (`toolsToLightPrompt`) вместо агрессивных "CALL A TOOL" правил. Спасает от бесконечных циклов. |
| **11** | Debug tracing middleware + max logging | `index.js, config.js, debug-trace.js` | Middleware ловит каждый запрос к `/chat*`, меряет тайминги, показывает ошибки и структуру ответа. LOG_LEVEL переключён на `'debug'`. Видно ГДЕ РОВНО падает. |
| **12** | logWarn import в routes.js | `routes.js` L25 | Добавлен `logWarn` в import из logger. Без этого — ReferenceError crash на streaming path (L1498). |
| **13** | Убран double-injection buildPayloadV2 | `chat.js` L519-523 | applyToolPrompt больше НЕ вызывается внутри sendMessage — system_message уже подготовлен в routes.js. Оставлен logDebug. |
| **14** | Transcript folding: natural language вместо JSON.stringify | `routes.js` L508-518 | assistant tool_calls → `"Assistant used tools: write_file"`, tool results → `"Tool output (write_file): ..."`. Qwen понимает контекст, не получает JSON-мусор. |

## Коммит-цепочка
```
93213f8 chore: remove unused Python/Docker/examples and old test scripts
03069ae fix: resolveQwenChatId return value, prevent chat spam on each request
1680b0e fix: handleApiError never returns undefined error, prevent silent swallowing in routes
84c91a6 fix: log API error body at WARN level for visibility
4e2dcd3 feat: auto-retry with fresh chat when Qwen returns 'chat not exist'
3f7567c feat: sliding window for agent-loop history + compact role logs to prevent crash
9468ef1 feat: fix tool_calls by using prompt injection instead of native OpenAI tools (+toolUtils.js)
[pushed]  fix: protocolTimeout, evaluate timeout, goto retry, safe sliding window pair preservation
ffb70ae feat: light tool prompt for agent-loop to break infinite tool call cycles
d6b4331 feat: add debug tracing middleware and max logging to find silent failures
37f3694 fix: import logWarn in routes.js (ReferenceError crash on streaming)
5abb2bd fix: prevent double tool prompt injection + allow reasoning text before JSON tool calls
db2f5bd fix: show folded transcript tool calls as natural language instead of raw JSON blob
6f0301f fix: transcript folding for tool calls (cleanup test files)
```

## Тестирование (статус)
- **Smoke test:** OK — `npm run smoke` прошёл.
- **Stability test:** OK — все 3 сценария пройдены.
- **Streaming with tools:** ⚠️ PROBLEMATIC — non-streaming path работает через curl корректно. Streaming capture mode возвращает ТОЛЬКО placeholder role:assistant + stop, без tool_calls content. Значит `captureToolCalls` branch (L1450 routes.js) не отправляет data chunk с инструментами при пустом raw content от Qwen.

## Отладка сессии 2026-06-07 (наблюдения)
### Что виделось в логах при реальном запросе от Zed Agent:
```
[1] Request POST /api/chat/completions → получен OpenAI-запрос со stream=true
[2] resolveQwenChatId → старый chat_dd679c0b9fd408a0 не существует (Qwen удалил)
    → AUTO-RETRY: создан новый чат a696a16a-b8c1-4f1a-87c6-153fbf77166e ✓
[3] sendMessage → модель qwen3.7-max, token OK, page pool OK
    → PAYLOAD V2 отправлен через Puppeteer fetch к /api/v2/chat/completions
    → Response получен успешно за ~5с ✓
[4] parseToolCallJson → модель вернула tool_call: list_directory ✓ (первые запросы)
[4b] parseToolCallJson → модель вернула plain text "Файл создан..." ✓ (повторные запросы с tool history)
    Маппинг сохранён, SSE первый чанк через 7.4s, ответ завершён status=200 ✓
```

### CAPTURE MODE debug output (chat.js L1263):
```
CAPTURE MODE: content type=string, len=303, preview="Файл 'D:\Projects\FreeQwenApi\test.py' создан...\nСодержимое..."
[TOOL_PARSE] input=Файл 'D:\Projects\FreeQwenApi\test.py' создан.
```

**ВЫВОД:** Прокси работает корректно (отправляет данные Qwen, получает ответ). Проблема: **Qwen НЕ генерирует JSON tool_calls когда в контексте есть tool history + folded transcript.** Модель видит "Assistant used tools" → "Tool result" и интерпретирует это как завершённый диалог → отвечает текстом вместо вызова инструмента.

Это проблема PROMPT/CONTEXT, не прокси-кода:
1. Qwen не понимает instruction "CALL A TOOL" при наличии tool results в истории
2. Light prompt (inAgentLoop=true) недостаточно агрессивен для повторных вызовов
3. Агрессивный prompt игнорируется — модель предпочитает ответить текстом

**Гипотеза:** проблема на стыке Qwen Chat behavior + agent-loop Zed:
- Прокси отправляет правильный OpenAI-compatible SSE (curl подтверждает)
- Zed Agent получает tool_calls но не исполняет ИЛИ не возвращает result обратно
- Без feedback loop от Zed → Qwen думает что всё сделано → отвечает текстом

## Открытые вопросы / отложенные фиксы
| # | Вопрос | Приоритет | Статус |
|---|--------|-----------|--------|
| O1 | ESLint: `removeInvalidToken` imported but unused в `chat.js` L12 | Low | Пре-экзистинг. Не трогал. |
| O2 | Node-streaming использует глобальный `fetch`. На старых Node может отсутствовать. | Medium | Fallback к browser fetch уже работает (L558). Проверить `typeof fetch` явно — TODO. |
| O3 | Память браузера растёт при agent-loop >100 вызовов. Нет max-age на страницы в пуле. | Medium | PAGE_POOL_SIZE=3 помогает, но старые страницы не GC-нуты пока в массиве. Добавить таймер — TODO. |
| ~~O4~~ | ~~Qwen tool calling НЕРАБОТАЕТ при Zed Agent~~ | **RESOLVED** | Проблема была в накопленном контексте на стороне Qwen (старые сессии/персонализация). После очистки памяти у Qwen и использования `qwen3-coder-plus` / чистого `max`, модель стабильно генерирует JSON tool_calls. Прокси парсит их через `parseToolCallJson()` и отдаёт Zed корректные `message.tool_calls`. |
| O5 | Streaming capture mode не отправляет reasoning text перед tool_calls SSE chunk'ами (extractReasoningText добавлен в routes.js но UNUSED). При `content: null` model → Zed получает пустой response. | Medium | TODO: обновить writeToolCallsSse + buildOpenAIToolResponse чтобы использовать extractReasoningText для отправки content delta ПЕРЕД tool_calls chunks. |
| ~~O6~~ | **Auto-Reset (Force Folding)** | **RESOLVED** | Реализовано: `forceResetModels` Set + `applyForceFolding()` сжимает 50+ сообщений до HEAD (оригинальная задача) + TAIL (текущее состояние) + суммарная статистика пропущенных tool calls. Флаг сбрасывается после применения.


- L31: `protocolTimeout: Number(process.env.PROTOCOL_TIMEOUT) || 600_000` — таймаут CDP-протокола увеличен до 10 минут.

### `src/api/chat.js`
- L88-104: новый `evaluateWithTimeout()` через `Promise.race` — health-check завершается за ≤5с вместо вечного ожидания.
- L145-166: retry-loop на `goto(CHAT_PAGE_URL)` — до 3 попыток с exponential backoff.
- **L519-523**: убран двойной вызов applyToolPrompt в buildPayloadV2. Оставлен logDebug.



### `src/api/routes.js`
- Sliding window (>60 msg) сохраняет пары `tool_call`/`tool_result`. Если после среза первая message — `role: "tool"`, добавляется assistant с `tool_calls`.
- Agent-loop detection через `hasOpenAIToolState(messages)` → переключение на light tool prompt.
- **buildStatelessTranscript L508-518**: assistant tool_calls → `"Assistant used tools: write_file"` вместо `JSON.stringify()`. Tool results → `"Tool output (write_file): ..."` вместо `"Tool result ({name}): ..."`.

### `src/debug-trace.js`
- Middleware отладки (остался в репозитории). Отключён в index.js — не подключается по умолчанию.

## Критический код — как работает сегодня (После очистки памяти Qwen - Session 9)
```
Zed Agent POST /api/chat/completions { messages: [...], tools: [...] }
  → routes.js парсит body, извлекает system message + combinedTools
    → applyToolPrompt(systemMessage, combinedTools, inAgentLoop) добавляет инструмент-схемы к системному промпту (НЕ к Qwen API!)
      → prepareOpenAIMessageInput() → shouldFoldOpenAITranscript() для agent-loop с tool history
        → buildStatelessTranscript(): "User: ...", "Assistant used tools: write_file", "Tool output (write_file): ..."
          → resolveQwenChatId(effectiveChatId, model) → маппинг generated→qwen ID. Если не существует — auto-retry с новым чатом.
            → sendMessage(messageContent, model, qwenChatId, ...) через Puppeteer fetch к /api/v2/chat/completions
              → executeApiRequest() → Qwen отвечает SSE stream (или JSON fallback)
                → если captureToolCalls=true: parseToolCallJson(content) → если JSON найден → OpenAI message.tool_calls format
                  → routes.js возвращает клиенту через buildOpenAIToolResponse() или writeToolCallsSse() (streaming)

СТАТУС: ✅ Работает стабильно при чистом контексте Qwen. 
(Накопленный контекст старых сессий у Макса вызывал отговоры текстом без JSON tool_calls).
```




## Сессия 8 (2026-06-07 — стабилизация tool calling + dedup)
**Проблема:** Zed Agent шлёт один и тот же запрос несколько раз за секунду. Прокси дергает Qwen многократно → модель возвращает дубликаты или "Tool X does not exists" бред из-за перегруженного промпта.

**Решение:**
1. Урезал `toolsToPrompt()` в `toolUtils.js`. Убрал 50+ строк схем и инструкций. Осталась жесткая инструкция: "Выведи JSON на последней строке". Это убирает галлюцинации модели про несуществующие тулы.
2. Внедрил Idempotency Cache (Dedup) в `routes.js`. Первый запрос кэшируется на 5 секунд. Повторные идентичные запросы от Zed сразу получают SSE-ответ из памяти без дергания Qwen.

**Изменения:**
### `src/api/toolUtils.js`
- `toolsToPrompt()`: сокращен с ~60 строк до ~12 строк. Только список имен тулов и жесткий формат JSON. Убраны verbose descriptions и excessive rules.

### `src/api/routes.js`
- Добавлен `idempotencyCache` (Map, TTL 5s).
- Инъекция дедупа в streaming блок: если идентичный запрос пришел <5с назад → мгновенная отправка кэшированного SSE `[DONE]`.
- Кэширование успешного результата Qwen после `sendMessage()` в streaming блоке.

**Коммит:** 410f55a (`fix: reduce tool prompt noise and deduplicate Zed retry requests`)

## Сессия 10 (текущая — 2026-06-08 — Force Folding)
**О6 решён**: Implemented Force Folding для автоматического сброса контекста Qwen.

**Как работает:**
1. `TOOL_CALL_RESET_THRESHOLD` (по дефолту 8) — после N tool calls, `invalidateModelDefaultChat(model)` инвалидирует чат и добавляет модель в `forceResetModels`
2. Следующий запрос Zed попадает в `prepareOpenAIMessageInput(messages, ..., model)`, которая проверяет флаг
3. Если флаг установлен: `applyForceFolding()` сжимает историю — первые 5 сообщений (оригинальная задача), последние 10 (текущее состояние), середина суммируется как статистика (`summarizeDiscardedTools()`)
4. Результат складывается через `buildStatelessTranscript` → компактный user message для свежего Qwen чата
5. Флаг сбрасывается — обычные запросы идут без сжатия

**Изменения в `src/api/routes.js`:**
- `forceResetModels` (Set) + `FORCE_FOLD_HEAD/TAIL` константы
- `applyForceFolding(messages, mappedModel)` — агрессивное сжатие истории
- `summarizeDiscardedTools(messages)` → `{role: "user", content: "[Previous agent work...]"}`
- `invalidateModelDefaultChat()` — добавлен `forceResetModels.add(model)`
- `prepareOpenAIMessageInput` — 4-й параметр `rawModel`, применяет Force Folding при наличии флага
- Оба обработчика (`/chat/completions`, `/v1/chat/completions`) передают `model`

## Сессия 9 (текущая — 2026-06-08)
### Сравнение с D:\FreeQwenApi (сторонний проект)
- **Одинаковый стек**: Node.js + Puppeteer. Оба используют `qwenTools = null`. 
- **Подтверждение ограничений**: другой проект тоже не шлет нативные OpenAI tools в payload Qwen API v2. Ошибка "Tool write_file does not exists" возникает на стороне самого API, когда мы пытались экспериментировать с нативным отправлением `tools[]`.

### Влияние модели и чистоты контекста Qwen
- **qwen3.7-max (с грязной памятью)**: при накопленном историческом контексте на стороне сайта Qwen (старые сессии, персонализация) модель игнорирует инструкцию "выдай JSON" и отвечает обычным текстом.
- **Фикс**: Очистка памяти на сайте Макса + использование `qwen3-coder-plus` / чистого `max`. Теперь модель стабильно генерирует минифицированный JSON tool_calls в ответе, который наш прокси парсит через `parseToolCallJson()` и успешно возвращает Zed.
- **Текущее состояние**: Tool calling работает. Прокси корректно перехватывает ответы Qwen SSE → парсинг JSON → OpenAI message.tool_calls format.

## Сессия 11 (текущая — 2026-06-08 — Refactor: Sub-router extraction + module split)
**Проблема**: `routes.js` ~2390 строк. Вся бизнес-логика сессий, маппинга чатов, парсинга OpenAI и SSE/response builders запихана в один файл.

**Выполнено:**
1. **Шаг 4 (дубли admin/media)** — уже сделан в предыдущем коммите. `adminRoutes` + `mediaRoutes` подключены через `router.use()`
2. **Шаг 1: Создать 3 новых модуля**:
   - `src/api/chatSession.js` (469 строк) — idempotency cache, chat ID resolution/generation/normalization, modelDefaultChats, forceFolding, session management
   - `src/api/openaiUtils.js` (207 строк) — parseOpenAIMessages, buildCombinedTools, stringifyOpenAIContent, buildStatelessTranscript, tool failure detection, folding decision, prepareOpenAIMessageInput
   - `src/api/responseBuilders.js` (225 строк) — buildOpenAIToolResponse, writeToolCallsSse, handleStreamingResponse, handleNonStreamingResponse
3. **Шаг 2: Обновить routes.js**:
   - Заменить inline-функции на импорты из новых модулей
   - Удалить ~680 строк дублирующегося кода (session helpers + openai utils + response builders)
   - `mapChatId` → `mapChatIdExport`, прямой доступ к `modelDefaultChats` → через `getModelDefaultChats()`
4. **Шаг 5-6 (объединение двух /chat/completions и fileRoutes)** — отложено до следующей сессии

**Результат:** routes.js: ~1020 строк (было ~2390). Логика чистая, модульная, без дубликатов.
**Валидация:** `node -c` на всех 4 файла + ESM import test — OK
**Не тронуты**: бизнес-логика хендлеров, sliding window, streaming, tool calls — только извлечение кода в отдельные файлы

**Коммит:** 58e0f87 (`Refactor routes: extract session, OpenAI utils, and response builders into modules`)
- новые файлы: `chatSession.js`, `openaiUtils.js`, `responseBuilders.js`
- удалено ~680 строк inline-кода из `routes.js`, заменено на импорты

## Сессия 12 (текущая — 2026-06-08 — Refactor: Dedup v1 handler + fileRoutes extraction)
**Остатки сессии 11 завершены:**

**Шаг 5:** Удален мертвый `/v1/chat/completions` хендлер (L920-L1420). URL-normalization middleware (`/\/v[12]/`) перенаправляет все v1-запросы на основной обработчик.

**Шаг 6:** Создан `src/api/fileRoutes.js` (101 строка) — вынесены:
- `/files/getstsToken`
- `/files/upload` (multer middleware)
- `/chats/:chatId/history` (GET + POST)
Подключен через `router.use(fileRoutes)` после auth.

**Фиксы попутно:**
- `/chat` endpoint использовал нелокализованную переменную `idemKey` (L219). Исправлено: добавлен `const chatIdemKey = getIdempotencyKey(...)`, idempotency check + dedup cache.
- `ALLOW_UNSCOPED_SESSION_CHAT_RESTORE` — использовался без import (артефакт сессии 11). Восстановлен импорт из `config.js`.

**Результат:** routes.js: ~929 строк (было ~2390 на входе в сессию 11). modules: chatSession + openaiUtils + responseBuilders + fileRoutes.
**Валидация:** `node -c` все файлы OK. ESM import chain подтвержден.

## Сессия 13 (текущая — 2026-06-08 — Refactor: chat.js split → pagePool module)
**Осуществлено:**

**Шаг 1:** Удалён unused импорт `removeInvalidToken` из `tokenManager.js` в `chat.js`. Убран `PAGE_POOL_SIZE` (теперь в `pagePool.js`).

**Шаг 2:** Создан `src/browser/pagePool.js` (221 строки) — вынесено:
- `createPage(context)` — создание Puppeteer page с поддержкой BrowserContext и Page ввода
- `evaluateWithTimeout(page, fn, timeoutMs)` — health-check обёртка для `page.evaluate()`
- `EVALUATE_HEALTH_TIMEOUT` константа (5000ms)
- `pagePool` объект — пул страниц с getPage/releasePage/clear/getStats
- `setAuthTokenGetter(fn)` — callback для проверки кэшированного токена при создании свежих страниц в пуле

**Шаг 3:** Обновлён `chat.js`:
- Удалены ~158 строк inline-кода (pagePool + getPage + evaluateWithTimeout)
- Добавлен re-export: `export { pagePool, createPage, evaluateWithTimeout } from "../browser/pagePool.js"`
- `extractAuthToken()` и `testToken()` → используют `createPage` вместо удалённого `getPage`
- Инициализация `setAuthTokenGetter(() => authToken)` — связывает пул с кэшем токена

**Шаг 4:** Обновлены импорты в зависимых файлах:
- `browser.js`: `clearPagePool()` → `pagePool.clear()`, импорт из `./pagePool.js`
- `fileUpload.js`: pagePool импорт → `../browser/pagePool.js` (was from chat.js)

**Результат:** chat.js: 1461 строк (было 1619, -158). modules: +pagePool.js. Циклическая зависимость browser ↔ chat сохранена через getAuthToken.
**Валидация:** `node -c` все файлы OK.

## Заметки для следующей сессии
- Не трогать `.rules` — sacred файл. Hands off.
- `edit_file` ломает код при nested replacements — используйте точные паттерны или checkout + single-change подход
- Прокси работает корректно как OpenAI-compatible API. Проблема Zed Agent ↔ Qwen Chat compatibility при agent-loop — частично решена упрощением промпта и дедупом.
- **Внимание:** `node -e "import('./src/api/routes.js')"` тянет browser → Chromium. Не использовать как validation tool.
